import {System} from "./System";
import {EntityManager} from "tiny-ecs";

export class UpdateConnectedStatusSystem implements System{

    process(_entities: EntityManager): void {
        // let coverage: {x:number, y:number}[] = [];

    }

}